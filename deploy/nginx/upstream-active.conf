# Active upstream: BLUE (default)
# Switch by replacing this file and running: docker exec am-nginx nginx -s reload
#
# Stable backend = active color (blue)
# Canary backend = inactive color (green)
upstream stable_backend {
    server am-app-blue:8080;
}
upstream canary_backend {
    server am-app-green:8080;
}
