# Active upstream: GREEN
# Switch by replacing upstream-active.conf and running: docker exec am-nginx nginx -s reload
#
# Stable backend = active color (green)
# Canary backend = inactive color (blue)
upstream stable_backend {
    server am-app-green:8080;
}
upstream canary_backend {
    server am-app-blue:8080;
}
